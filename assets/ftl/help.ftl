You can type next commands:
select user where id = {userID}, {fields what you want select, e.g.: name, wins, playedGames}
select users, {fields what you want select, e.g.: name, wins, playedGames}
select users, wins